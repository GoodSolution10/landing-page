# landing-page

Landing page da GoodSolution — agentes de IA para atendimento no WhatsApp.

- Site estático (1 arquivo `index.html`), servido por nginx via Dockerfile.
- Deploy: Coolify na VPS, conectado a este repo (push na `main` = deploy).
